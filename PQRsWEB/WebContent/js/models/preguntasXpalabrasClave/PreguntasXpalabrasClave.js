/**
 * 
 */
define([
  'underscore',
  'backbone'
], function($, _,Backbone, PreguntasXpalabrasClaveModel) {
  
  var PreguntasXpalabrasClaveModel = Backbone.Model.extend({});

  return PreguntasXpalabrasClaveModel;

});